import React, { Component } from 'react'
import ReactDOM from 'react-dom'

const styles = {
  corpoBello: {
    paddingTop: 40,
    textAlign: 'center',
  </style>
  },
}

export default class Section extends Component {
  render() {
    return (
      <div style={styles.corpoBello}>
        Section!!!
      </div>
    )
  }
}